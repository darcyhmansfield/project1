<h1>All Teams</h1>

<% @teams.each do |team| %>
    <h2><%= team.name %></h2>
    <% team.wrestlers.each do |wrestler| %>
        <%= link_to image_tag(wrestler.image, :class => 'thumb'), wrestler %>
    <% end %>
<% end %>